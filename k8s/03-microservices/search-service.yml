apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: viajesgvr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      containers:
        - name: search-service
          image: gianellovr/viajesgvr-search-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: viajesgvr-common-config
          env:
            - name: SPRING_APPLICATION_NAME
              value: "search-service"
            - name: DB_URL
              value: "jdbc:mysql://mysql-service:3306/db_viajesgvr_search?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: viajesgvr-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: viajesgvr-secrets
                  key: DB_PASSWORD
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql-service:3306/db_viajesgvr_search?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: viajesgvr-secrets
                  key: DB_USERNAME
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: viajesgvr-secrets
                  key: DB_PASSWORD
            - name: EUREKA_INSTANCE_HOSTNAME
              value: "search-service"
            - name: EUREKA_INSTANCE_INSTANCE_ID
              value: "search-service:8080"
            - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
              value: "false"
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: viajesgvr
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
    - port: 8080
      targetPort: 8080