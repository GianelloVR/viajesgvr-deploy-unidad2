apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-mysql
  namespace: viajesgvr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-mysql
  template:
    metadata:
      labels:
        app: payment-mysql
    spec:
      containers:
        - name: payment-mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: viajesgvr-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: db_viajesgvr_payment
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: payment-mysql
  namespace: viajesgvr
spec:
  type: ClusterIP
  selector:
    app: payment-mysql
  ports:
    - port: 3306
      targetPort: 3306