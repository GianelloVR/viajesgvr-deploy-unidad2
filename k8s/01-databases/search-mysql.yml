apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-mysql
  namespace: viajesgvr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-mysql
  template:
    metadata:
      labels:
        app: search-mysql
    spec:
      containers:
        - name: search-mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: viajesgvr-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: db_viajesgvr_search
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: search-mysql
  namespace: viajesgvr
spec:
  type: ClusterIP
  selector:
    app: search-mysql
  ports:
    - port: 3306
      targetPort: 3306